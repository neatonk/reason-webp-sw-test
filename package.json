{
  "name": "reason-webp-sw-test",
  "private": true,
  "devDependencies": {
    "bs-platform": "1.7.x",
    "http-server": "0.10.x"
  },
  "scripts": {
    "preinstall": "(cd .git/hooks/ && ln -s ../../hooks/* .)",
    "build": "bsb",
    "watch": "bsb -w",
    "serve": "http-server -o",
    "test": "node lib/js/test/hello.js"
  }
}
